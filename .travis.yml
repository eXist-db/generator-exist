language: node_js

node_js:
  - "node"
  - "8"
  - "6"

# I m not sure if this is helping
cache:
  directories:
    - "node_modules"

before_install:
  - npm prune

install:
  - npm install -q


# after_success:
#   - npm run semantic-release

notifications:
  email: false
